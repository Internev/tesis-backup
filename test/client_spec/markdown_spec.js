// <!-- Created by Duncan on 12.28.2016 -->
const Vue = require('vue');
const Markdown = require('../../client/vue-components/markdown.vue');

describe('Markdown', () => {
  it('should exist', () => {
    expect(typeof Markdown).toBe('object');
  })
});